function merge(arr1: number[], arr2: number[]): number[] {
    let i1 = 0; // arr1 を参照するインデックス
    let i2 = 0; // arr2 を参照するインデックス
  
    let result = [];
    while (result.length < arr1.length + arr2.length) {
       // ???
    }
  
    return result;
  }
  //merge([3, 4, 9], [4, 10, 11]); // [3, 4, 4, 9, 10, 11]となるように